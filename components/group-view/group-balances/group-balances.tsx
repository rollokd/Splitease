import React from "react";

type Props = {};

const groupBalances = (props: Props) => {
  // TODO: fetch all individual balances for the group

  return <div>groupBalances</div>;
};

export default groupBalances;
